{
  "langrev": "1",
  "warnings": [ "no-unused" ],
  "dependency-search-paths": [ "lib" ],
  "dependencies": [ ],
  "authors": [ "Nomota <hiongun@gmail.com>" ],
  "version": "0.1.0",
  "sources": [ "src/**" ],
  "test-sources": [ "test/**" ],
  // "c-sources": [ "csource/**" ],
  // "c-include-dirs": [ "csource/include" ],.
  "build-dir": "build",
  "output": "build",
  // 'c3c --list-targets' 
  // "target": "windows-x64",
  "targets": {
    "logd": {
      "type": "executable",
      // Additional libraries, sources
      // overrides of global settings here.
    },
  },
  "cpu": "generic",
  // Optimization: "O0" .. "O5", "Os", "Oz".
  "opt": "O0"
}
